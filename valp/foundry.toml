[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
optimizer = true
optimizer_runs = 200
fuzz = { runs = 1000 }
invariant = { runs = 256, depth = 100 }

[profile.ci]
fuzz = { runs = 5000 }
invariant = { runs = 512, depth = 256 }
